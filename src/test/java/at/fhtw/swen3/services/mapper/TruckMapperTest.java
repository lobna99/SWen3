package at.fhtw.swen3.services.mapper;

import at.fhtw.swen3.persistence.entities.TruckEntity;
import at.fhtw.swen3.services.dto.Truck;
import org.junit.jupiter.api.Test;

import java.text.ParseException;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class TruckMapperTest {
    /*
    @Test
    public void testEntityToDto() throws ParseException {
        // Create a TruckEntity object
        TruckEntity entity = new TruckEntity();
        entity.setCode("ShjG3");
        entity.setDescription("This is a description");

        // Convert the entity to a DTO
        Truck dto = TruckMapper.INSTANCE.entityToDto(entity);

        // Assert that the properties of the DTO match the properties of the entity
        assertEquals(entity.getCode(), dto.getCode());
        assertEquals(entity.getDescription(), dto.getDescription());
    }

    @Test
    public void testDtoToEntity() throws ParseException {
        // Create a Truck DTO
        Truck dto = new Truck();
        dto.setCode("ShjG3");
        dto.setDescription("This is a description");

        // Convert the DTO to an entity
        TruckEntity entity = TruckMapper.INSTANCE.dtoToEntity(dto);

        // Assert that the properties of the entity match the properties of the DTO
        assertEquals(dto.getCode(), entity.getCode());
        assertEquals(dto.getDescription(), entity.getDescription());
    }

     */

}